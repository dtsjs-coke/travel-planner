# 2026-09-10 작업 로그

## 모바일 Day 간 드래그 앤 드롭 — 설계 확정 + 구조 변경 (senior-dev)

담당: senior-dev (오케스트레이터 위임). 사용자 후속 요청:
**"PC에서는 날짜가 탭으로 나뉘어있으니깐 해당 기능(드롭다운)처럼 해도될거같은데, 모바일에서는 드래그로도 옮겨지면 좋겠어."**

즉 어제(2026-09-09~10) 완성한 "일정을 다른 Day로 옮기기"에서 senior-dev가 의도적으로 배제했던
드래그 앤 드롭(ADR-0004 기각 대안)을 **모바일에 한해서만** 도입하는 요청.
이번 위임 범위는 **설계 + 상태/구조 변경까지**이고, 시각 효과·UI 세부 구현은 ui-dev 후속.

### 먼저 확인한 것 (코드 기준)

- `DndContext`와 `SortableContext`가 **둘 다 `ItineraryList` 안**에 있었다. 모바일은 Day 섹션마다
  `ItineraryList`를 렌더하므로 **Day 개수만큼 독립된 DndContext**가 존재했고, dnd-kit은 DndContext
  경계를 넘는 드래그를 지원하지 않으므로 **현재 구조에서는 Day 간 드래그가 원천적으로 불가능**했다.
  → 예상대로 "여러 Day를 아우르는 단일 DndContext로 재구성"이 이 기능의 전제조건.
- 자동 스크롤: `@dnd-kit/core` 6.3.1의 `DndContext`가 `autoScroll?: boolean | AutoScrollOptions`를
  **기본 활성**으로 내장 지원(`node_modules/@dnd-kit/core/dist/components/DndContext/DndContext.d.ts`,
  `hooks/utilities/useAutoScroller.d.ts`의 `threshold`/`acceleration`/`activator` 옵션 확인).
  모바일 뷰는 window를 스크롤하므로 **별도 구현이 필요 없다.**
- 터치 센서는 이미 `PointerSensor{distance:5}` + `TouchSensor{delay:150, tolerance:5}`로 M3(2026-09-04)에서
  튜닝돼 있었고, 드래그 리스너는 핸들(`⠿`, `touch-none`)에만 붙어 있어 제목 편집/스크롤과 분리돼 있었다.
- `useActiveDayOnScroll`은 스크롤 위치를 읽기만 하므로 드래그와 상태 충돌이 없다.

### 결정 (상세는 [ADR-0005](../docs/adr/0005-mobile-cross-day-drag-and-drop.md))

1. **모바일에만** Day 간 드래그. 데스크톱은 코드·동작 그대로(드롭다운 유지).
2. `DndContext` 소유권을 `ItineraryList` → 상위 뷰(`DayEditorDesktop`/`DayEditorMobile`)로 이동.
   모바일 = Day1~DayN 전체를 감싸는 1개, 데스크톱 = 활성 Day 하나를 감싸는 1개(**트리 모양이 변경 전과 동일**).
3. 드롭 해석은 `lib/dndDrop.ts`의 순수 함수 `resolveDrop()` 단일 소스(`reorder` / `move` / `null`).
4. **빈 Day에만** 컨테이너 droppable(`day:<id>`) 등록 — 항목이 없으면 드롭 대상 자체가 없어서 영원히
   옮겨올 수 없는 Day가 생기기 때문. 항목이 있는 Day는 항목 droppable만으로 충분.
5. 충돌 판정은 기존 `closestCenter` 유지(4번 덕에 컨테이너 ↔ 항목 경합이 없어 커스텀 캐스케이드 불필요).
6. 다른 Day로 드롭 = 그 Day **맨 뒤**에 삽입. 기존 `POST /api/items/{id}/move` 그대로 → **백엔드 변경 없음**.
   `target_position`은 추가하지 않고, 삽입선 대신 **목적지 Day 섹션 전체 하이라이트**로 표시.
7. 자동 스크롤은 dnd-kit 내장(기본값) 사용.
8. `TouchSensor` 값·핸들 전용 리스너는 **무변경**(검증된 값을 구조 변경과 같은 세션에 바꾸지 않는다).
9. **`MoveItemControl`(하단 시트) 유지** — 먼 Day 이동은 드래그가 비싸고, 드래그는 실패 가능한 제스처라
   단독 수단이 될 수 없다. 데스크톱과 컴포넌트를 공유 중이라 떼어내면 오히려 분기가 는다.
10. 낙관적 업데이트는 여전히 없음. 드래그 이동에서는 `jumpToDay`(목적지 자동 스크롤)를 **호출하지 않는다**
    — 이미 목적지를 보면서 떨궜는데 화면이 또 튀면 방해.

### 실제로 변경한 파일 (프론트 구조만, 시각 효과 없음)

- **신규** `frontend/src/lib/dndDrop.ts` — `dayDroppableId()`, `resolveDayIdOfDropTarget()`, `resolveDrop()`.
  항목 droppable id는 숫자(`item.id`), Day 컨테이너는 문자열(`day:<dayId>`)로 id 공간 분리.
- **신규** `frontend/src/hooks/useItineraryDndSensors.ts` — `ItineraryList`에 있던 센서 구성을 그대로 이동(값 무변경).
- `frontend/src/components/ItineraryList.tsx` — `DndContext`/센서/`handleDragEnd` 제거, `SortableContext` + 목록만 담당.
  props에 `dayId` 추가, `onReorder` 제거. `items.length === 0`일 때 빈 상태 문구에 `useDroppable`의 `setNodeRef` 부착
  (드롭 영역 확보용으로 `py-3`만 추가).
- `frontend/src/components/DayEditorDesktop.tsx` — 활성 Day 하나를 감싸는 `DndContext` 추가, `handleDragEnd`가
  `resolveDrop(..., { [activeDayId]: items })` 결과로 기존 `onReorderItems`를 호출. `DndContext`는 DOM 노드를
  만들지 않아 2단 그리드 레이아웃에 영향 없음.
- `frontend/src/components/DayEditorMobile.tsx` — 전체를 감싸는 `DndContext`(autoScroll 기본값) 추가,
  `onDragOver`에서 `resolveDrop()` 결과가 `move`일 때만 `dropTargetDayId`를 세팅하고 각 `<section>`에
  `data-drop-target="true"`로 노출(스타일은 ui-dev 몫), `onDragEnd`에서 reorder면 `onReorderItems`,
  move면 `onMoveItem(fromDayId, itemId, toDayId)`(=`jumpToDay` 미전달).
- **무변경**: `ItineraryItemCard.tsx`(핸들/편집/삭제/`MoveItemControl` 그대로), `MoveItemControl.tsx`,
  `DayEditorPage.tsx`(mutation·에러 처리 그대로), `types/dayEditor.ts`, `api/items.ts`, 백엔드 전체.

### 검증

- `npm run build`(`tsc -b && vite build`) 통과 — 타입 에러 없음.
- `npm run lint`(oxlint) — 기존 경고 3건(AuthContext/MapView) 그대로, **신규 파일·변경 파일에서 새 경고 없음**.
- `resolveDrop()` 단위 검증 14케이스 통과 (`node --experimental-strip-types`로 실제 `.ts` 파일을 직접 임포트해 실행):
  - **같은 Day 정렬 회귀**: 위→아래/아래→위 `arrayMove` 결과, 자기 자신 위 드롭 → null, `over` 없음/null → null,
    데스크톱처럼 Day 하나만 넘긴 컨텍스트 → 기존과 동일한 `orderedItemIds`.
  - **Day 간 이동**: 다른 Day 항목 위 드롭 → `{kind:'move', fromDayId, toDayId}`, 빈 Day 컨테이너(`day:3`) 드롭 → move,
    자기 Day 컨테이너 드롭 → null(no-op).
  - **방어**: 없는 항목 id, 없는 대상 id, 깨진 컨테이너 id(`day:abc`) → 전부 null.
- 백엔드는 손대지 않았으므로 서버 기동/DB 접근 없음(`dev.db` 무변경).
- **브라우저·실기 터치 확인은 미수행**(자동화 도구 없음 + 시각 구현이 아직 ui-dev 몫이라 지금 확인해도 반쪽).

### ui-dev 후속 구현 스펙 (이 세션에서 넘기는 작업)

아래만 하면 되고, **`lib/dndDrop.ts` / DndContext 배치 / API 호출 방식은 건드리지 말 것**(ADR-0005 결정 사항).

1. **드롭 대상 Day 하이라이트** — `DayEditorMobile`의 각 `<section>`에 이미 `data-drop-target="true"`가
   드래그 중에만 붙는다. 여기에 눈에 띄는 테두리/배경(예: `data-[drop-target]:ring-2 ring-slate-400 rounded-lg`)을 준다.
   **삽입선(insertion line)은 그리지 말 것** — 실제 삽입 위치는 항상 맨 뒤라서 거짓말이 된다.
   빈 Day의 "등록된 일정이 없습니다" 영역도 이 하이라이트에 포함되도록(섹션 단위라 자동으로 포함됨) 확인.
2. **`DragOverlay` 추가(모바일)** — 현재는 카드가 제자리에서 transform만 되고 `opacity: 0.5`라,
   상단 고정 지도 블록(`sticky ... z-10`) 밑으로 들어가면 **드래그 중인 카드가 지도에 가려 보이지 않는다.**
   `DayEditorMobile`의 `DndContext` 안에 `<DragOverlay>`를 두고 `onDragStart`에서 잡은 항목의 간단한 카드
   프리뷰를 렌더할 것(DragOverlay는 기본 `z-index: 999`라 지도 위로 뜬다). 원본 카드는 지금처럼 흐리게 유지.
3. **이동 실패 피드백 위치(모바일)** — 낙관적 업데이트가 없어서 이동이 실패하면 **아무 일도 안 일어난 것처럼 보인다.**
   `DayEditorPage`의 `moveItemError`는 화면 최상단에 있어 리스트 아래쪽에서 드래그한 사용자에게 안 보인다.
   모바일일 때는 하단 고정 배너(예: `fixed bottom-4 inset-x-4 z-40`, 탭하면 닫힘)로 띄울 것.
   상태(`moveItemError`)와 mutation은 그대로 쓰고 **표시 위치만** 바꾼다.
4. **실기 터치 확인 후 필요하면 자동 스크롤만 미세조정** — `DndContext`에
   `autoScroll={{ threshold: { x: 0, y: 0.2~0.3 } }}` 정도의 값 조정만 허용(직접 구현 금지).
   상단 고정 블록이 뷰포트 상단 40% 가까이를 덮고 있어 위쪽 자동 스크롤 발동 지점이 지도 아래에 있을 수 있다.
5. **하지 말 것**: `TouchSensor` 값 변경(M3 검증값), 드래그 중 다른 Day 항목을 비켜서게 하는 크로스 컨테이너
   프리뷰(ADR-0005에서 기각 — 로컬 상태 사본이 생겨 같은-Day 정렬이 회귀 위험에 노출됨), `MoveItemControl` 제거.

### 이슈/특이사항

- `ItineraryList`가 더 이상 자기 `DndContext`를 갖지 않는다. 앞으로 이 컴포넌트를 다른 화면에서 재사용하려면
  **호출부가 DndContext를 제공**해야 한다(잊으면 dnd-kit이 즉시 에러를 던지므로 조용히 깨지진 않는다).
- 백엔드는 이번 요청으로 바뀐 게 없다. 운영 Neon/Render에 이번 건으로 조치할 것도 없다.

### 산출물(변경 파일)

- `frontend/src/lib/dndDrop.ts` (신규)
- `frontend/src/hooks/useItineraryDndSensors.ts` (신규)
- `frontend/src/components/ItineraryList.tsx`
- `frontend/src/components/DayEditorDesktop.tsx`
- `frontend/src/components/DayEditorMobile.tsx`
- `docs/adr/0005-mobile-cross-day-drag-and-drop.md` (신규)
- `docs/PROJECT.md` (Status / Backlog Tier 1 / Architecture Decisions / Milestone Log)
- `logs/2026-09-10.md`(본 파일), `logs/MASTER_LOG.md`

### 사용자 확인/조치 필요

- [ ] **지금 상태로는 화면에 변화가 없다** — 구조만 바꿔둔 단계라 드래그로 Day를 옮길 수는 있게 됐지만
      "여기에 떨어진다"는 시각 표시가 없고, 드래그 중인 카드가 상단 지도에 가려진다. **ui-dev 후속 작업이
      끝난 뒤 실기(안드로이드 크롬)에서 확인**하는 것을 권장.
- [ ] **실기 터치 확인 시 볼 것**: ① 핸들을 짧게 잡고 세로로 밀 때 페이지 스크롤/당겨서 새로고침이 여전히
      정상인지, ② 핸들을 누른 채 화면 아래쪽 끝으로 가져가면 자동 스크롤이 걸리는지, ③ 다른 Day 섹션에
      떨궜을 때 그 Day 맨 뒤에 붙는지, ④ 같은 Day 안에서의 기존 드래그 정렬이 그대로인지(회귀).
- [ ] **git 커밋 안 함** (CLAUDE.md 규칙 — 명시적 요청 시에만). 커밋은 ui-dev 작업까지 끝낸 뒤 한 번에 권장.
- [ ] 데스크톱은 동작이 바뀌지 않아야 한다 — 코드상 트리 구조가 동일하지만, PC에서 **드래그 정렬 한 번만**
      해보고 이상 없는지 확인해두면 이번 구조 변경의 회귀를 완전히 배제할 수 있다.

## 모바일 Day 간 드래그 앤 드롭 — 시각/UI 구현 (ui-dev)

담당: ui-dev (오케스트레이터 위임). 위 senior-dev 스펙(ADR-0005 결정 6/7 + 이 로그의 "ui-dev 후속 구현 스펙" 4개 항목) 그대로 구현.
`lib/dndDrop.ts`의 `resolveDrop()`, `DndContext` 배치(누가 감싸는지), `TouchSensor` 값, `MoveItemControl.tsx`는 전혀 건드리지 않음(금지 사항 그대로 준수).

### 변경 내용

1. **드롭 대상 하이라이트** (`DayEditorMobile.tsx`) — 각 Day `<section>`에 이미 배선돼 있던
   `data-drop-target="true"`(드래그 중이고 `resolveDrop()` 결과가 `move`일 때만 세팅)에
   `data-[drop-target]:bg-slate-50 data-[drop-target]:ring-2 data-[drop-target]:ring-slate-400`
   (+ `rounded-lg`, `transition-colors`, `-mx-2 px-2`로 화면 좌우 여백에 맞춰 하이라이트 박스 폭 조정)를 추가.
   Tailwind v4의 데이터 속성 "값 무관 존재 여부" variant(`data-[drop-target]:`)를 사용 — 속성이 없을 때(정렬 중이거나
   드래그가 없을 때)는 자동으로 스타일이 빠진다. **삽입선은 그리지 않음** — 실제 삽입은 항상 그 Day의 맨 뒤이므로,
   특정 위치를 가리키는 표시는 senior-dev 스펙대로 의도적으로 배제(폴더에 파일을 떨구는 상호작용으로 통일).
   빈 Day의 "등록된 일정이 없습니다" 문구도 섹션 안에 있어 자동으로 하이라이트에 포함됨.

2. **`DragOverlay` 추가** (`DayEditorMobile.tsx`) — `onDragStart`에서 `event.active.id`(숫자, 항목 id)로
   `itemsByDayId`를 순회해 해당 항목과 그 Day 안에서의 index를 찾아 `activeDragItem` state에 저장.
   `<DndContext>` 안에 `<DragOverlay>{activeDragItem && <ItineraryItemDragPreview .../>}</DragOverlay>`를 추가.
   `ItineraryItemDragPreview`는 파일 내부에 새로 만든 프리젠테이션 전용 컴포넌트로, `ItineraryItemCard.tsx`의
   카드 배치(핸들 아이콘 ⠿ + 순번 배지 + 제목, `rounded-lg bg-white p-4 shadow`)를 그대로 재사용하되
   편집/삭제/`MoveItemControl` 같은 상호작용은 없음(드래그 중 "무엇을 옮기는지"만 보여주면 되므로).
   루트 태그는 `<li>`가 아니라 `<div>` — DragOverlay는 `<ol>` 트리 밖(포탈)에 렌더되므로 `<li>`를 쓰면 잘못된
   HTML 중첩이 된다. dnd-kit `DragOverlay`의 기본 `z-index: 999`가 상단 `sticky z-10` 지도보다 높아서,
   카드를 지도 영역까지 끌고 올라가도 더 이상 가려지지 않는다. 원본 카드는 기존 그대로 `opacity: 0.5`로 제자리에 남는다.
   `handleDragEnd`/`handleDragCancel`(신규, 기존엔 인라인 화살표 함수였던 것을 이름 붙은 함수로 분리)에서
   `activeDragItem`도 `dropTargetDayId`와 함께 초기화.

3. **이동 실패 에러 배너 위치** (`DayEditorPage.tsx`, 모바일 전용) — 기존 `{moveItemError && (...)}` 한 블록을
   `isDesktop`(이미 `useIsDesktop()`으로 계산돼 있던 값) 기준으로 둘로 나눔: 데스크톱은 기존 위치(폼 아래
   상단 배너) 그대로, 모바일은 `fixed inset-x-4 bottom-4 z-40` 버튼으로 화면 하단 고정 — 탭하면 기존
   `setMoveItemError(null)`을 그대로 호출해 닫힘. **상태(`moveItemError`)와 `moveItemMutation` 로직은 전혀
   손대지 않았고, 표시 위치(레이아웃)만 분기**했다.

4. **`autoScroll` 값 조정** (`DayEditorMobile.tsx`) — `<DndContext autoScroll={{ threshold: { x: 0, y: 0.3 } }}>`.
   상단 `sticky` 지도 블록(`MapView` height 260px + Day 칩 행, `ResizeObserver`로 측정하는 `stickyHeight`,
   기본값만 봐도 320px 안팎)이 일반적인 모바일 뷰포트 위쪽 상당 부분을 덮어서, dnd-kit 기본 임계값(위/아래 각
   20%)의 위쪽 발동 지점이 지도 뒤에 가려 손가락이 닿지 않는 문제가 있었다. 가로 자동 스크롤은 애초에 쓰지
   않으므로 `x: 0`으로 비활성화하고, 세로만 senior-dev가 제시한 범위(`0.2~0.3`) 중 `0.3`(지도가 큰 화면도
   커버하도록 넉넉한 쪽)으로 조정. **`stickyHeight`/`window.innerHeight` 기반 동적 계산은 하지 않음** — "직접
   스크롤 로직 구현 금지" 지시를 넓게 해석해 dnd-kit 파라미터 하나를 정적 값으로만 바꿨다(동적 계산도 여전히
   dnd-kit 옵션에 값을 넘기는 것뿐이라 스펙 위반은 아니라고 보이지만, 실기 확인 없이 판단하기 애매해 보수적으로
   정적 값을 택함 — 실기 확인 후 필요하면 조정 권장).

### 회귀 확인

- 코드 리딩 기준: 데스크톱 관련 변경은 `DayEditorPage.tsx`의 `moveItemError` 배너 분기 하나뿐이고
  `isDesktop === true`일 때의 분기(`{moveItemError && isDesktop && (...)}`)는 기존 JSX와 완전히 동일 —
  `DayEditorDesktop.tsx`, `ItineraryList.tsx`, `ItineraryItemCard.tsx`, `MoveItemControl.tsx`, `lib/dndDrop.ts`는
  이번 작업에서 전혀 수정하지 않음.
- `npm run build`(`tsc -b && vite build`) 통과 — 타입 에러 없음.
- `npm run lint`(oxlint) — 기존 경고 3건(`AuthContext.tsx` react/only-export-components,
  `MapView.tsx` react-hooks/exhaustive-deps 2건) 그대로, 신규/변경 파일에서 새 경고 없음.
- **브라우저·실기 터치 확인은 미수행**(자동화 도구 없음 — 하이라이트가 실제로 눈에 띄는지, `DragOverlay`가
  지도 위로 정말 뜨는지, 하단 배너가 다른 UI와 겹치지 않는지, 자동 스크롤 임계값 `0.3`이 실기에서 적당한지는
  실기 확인이 필요하다).

### 산출물(변경 파일)

- `frontend/src/components/DayEditorMobile.tsx` (하이라이트 클래스, `DragOverlay`+`ItineraryItemDragPreview`,
  `autoScroll` 값, `handleDragStart`/`handleDragCancel` 추가)
- `frontend/src/routes/DayEditorPage.tsx` (`moveItemError` 배너를 `isDesktop` 기준으로 데스크톱/모바일 분기)
- `docs/PROJECT.md` (Status, Backlog Tier 1 항목 `[x]` 갱신, Milestone Log)
- `logs/2026-09-10.md`(본 파일)

### 사용자 확인/조치 필요

- [ ] **실기(안드로이드 크롬)에서 확인할 것**: ① 다른 Day 섹션 위로 드래그하면 배경/테두리 하이라이트가 뜨는지,
      ② 드래그 중인 카드가 상단 지도 영역까지 올라가도 카드가 계속 보이는지(더 이상 지도에 가려지지 않는지),
      ③ 목록 하단에서 드래그하다 실패시켰을 때 에러 배너가 화면 하단에 보이고 탭하면 닫히는지,
      ④ 화면 아래쪽 끝까지 손가락을 가져갔을 때 자동 스크롤이 지도 아래 지점부터 자연스럽게 걸리는지
      (`y: 0.3`이 너무 이르거나 늦다고 느껴지면 이 값만 조정하면 됨).
- [ ] PC에서 **드래그 정렬 한 번, 카드 편집/삭제/`MoveItemControl` 드롭다운 한 번씩** 확인 권장(데스크톱은
      코드상 변경이 없지만 senior-dev 세션 회귀 확인 항목과 동일하게 한 번 더 짚어두면 안전).
- [ ] **git 커밋 안 함** (CLAUDE.md 규칙 — 명시적 요청 시에만). senior-dev 구조 변경 + 이번 시각 구현을
      한 번에 커밋하는 것을 권장.

## 모바일 Day 간 드래그 앤 드롭 — QA 검증 (qa-reviewer)

담당: qa-reviewer (오케스트레이터 위임). senior-dev의 구조 변경(ADR-0005)과 ui-dev의 시각 구현을 코드 리딩 +
`git show HEAD:...`를 통한 변경 전/후 비교 + `resolveDrop()` Node 재현 스크립트로 검증. 이 세션에서 커밋된
것은 없으므로 비교 기준은 마지막 커밋(`1df812a`)이다.

### 1. 데스크톱 회귀 검증

- `git show HEAD:frontend/src/components/ItineraryList.tsx`(변경 전)와 `DayEditorDesktop.tsx`(변경 전)를
  현재 코드와 직접 대조.
  - 센서: 변경 전 `PointerSensor{distance:5}` + `TouchSensor{delay:150,tolerance:5}` → 새 `useItineraryDndSensors()`와
    값 동일(`frontend/src/hooks/useItineraryDndSensors.ts:11-12`). 일치.
  - `collisionDetection`: 변경 전/후 모두 `closestCenter`. 일치.
  - 같은-Day 정렬 결과: 변경 전 `handleDragEnd`는 `oldIndex`/`newIndex` → `arrayMove` → `onReorder(ids)` →
    `onReorderItems(activeDayId, ids)`. 새 코드는 `resolveDrop()`이 동일한 `oldIndex`/`newIndex`/`arrayMove` 로직을
    수행하고(`dndDrop.ts:72-81`) `handleDragEnd`가 `onReorderItems(result.dayId, result.orderedItemIds)`를 호출
    (`DayEditorDesktop.tsx:31-32`). 데스크톱은 `{ [activeDayId]: items }` 한 Day만 넘기므로 `result.dayId`는
    항상 `activeDayId`와 같음 — 결과적으로 mutation 호출 시그니처가 이전과 동일. `reorderMutation`의 낙관적
    업데이트(`DayEditorPage.tsx:88-113`)는 이번 세션에서 손대지 않음(diff에 없음). **회귀 없음.**
  - `DndContext` 개수: 데스크톱은 `DayEditorDesktop.tsx`의 `<div className="grid ...">` 안에 정확히 1개
    (`DayEditorDesktop.tsx:66`)만 존재. 그 안에 `ItineraryList`가 렌더하는 `SortableContext`가 정확히 1개.
    ADR-0005의 "트리 모양이 변경 전과 동일하다" 주장은 **항목이 있는 Day에 한해서는** 사실.
- **사소한 발견 — ADR 문구가 완전히 정확하지는 않음(엣지 케이스)**: 변경 전에는 `DndContext`가 `ItineraryList`
  내부에 있었고, 그 컴포넌트 자신이 `items.length === 0`일 때 `<DndContext>`를 아예 만들지 않고 `<p>`만
  반환했다(`git show HEAD:...ItineraryList.tsx` 확인). 반면 변경 후에는 `DndContext`가 `DayEditorDesktop`으로
  올라가면서 조건이 `!itemsLoading`으로 바뀌어, **선택한 Day에 항목이 0개여도 `DndContext`가 항상 마운트된다**
  (`DayEditorDesktop.tsx:66` — `items.length` 조건 없음). 즉 빈 Day 탭으로 전환하면 이전엔 `DndContext`가
  언마운트됐지만 지금은 계속 떠 있다. 드래그할 항목 자체가 없어 사용자 동작에는 영향이 없고 API 호출도
  없으므로 **기능적 회귀는 아니지만**, "데스크톱 트리 모양이 변경 전과 완전히 동일하다"는 ADR-0005 결정 2의
  문구는 이 엣지 케이스에서는 부정확함. 심각도: **사소** (문서 정확성 문제, 동작에는 영향 없음).

### 2. `resolveDrop()` 검증

`frontend/src/lib/dndDrop.ts`를 Node(`--experimental-strip-types`)로 직접 임포트해 13개 케이스를 재현
(스크래치패드 스크립트, 프로젝트에는 남기지 않음). 전부 코드가 문서/스펙대로 동작함을 확인.

| 케이스 | 입력 | 결과 |
|---|---|---|
| 같은 Day 재정렬(아래로) | `resolveDrop(101, 103, {...})` | `{kind:'reorder', dayId:1, orderedItemIds:[102,103,101]}` ✅ |
| 같은 Day 재정렬(위로) | `resolveDrop(103, 101, {...})` | `{kind:'reorder', dayId:1, orderedItemIds:[103,101,102]}` ✅ |
| 다른 Day 항목 위 드롭 | `resolveDrop(101, 201, {...})` | `{kind:'move', itemId:101, fromDayId:1, toDayId:2}` ✅ |
| 빈 Day 컨테이너(`day:3`) 드롭 | `resolveDrop(101, dayDroppableId(3), {...})` | `{kind:'move', ..., toDayId:3}` ✅ |
| 자기 Day 컨테이너 드롭(no-op) | `resolveDrop(101, dayDroppableId(1), {...})` | `null` ✅ |
| 자기 자신 위 드롭 | `resolveDrop(101, 101, {...})` | `null` ✅ |
| `over` 없음(`undefined`/`null`) | `resolveDrop(101, undefined/null, {...})` | `null` ✅ (취소/대상 없음 시 API 호출 없음) |
| 존재하지 않는 `active` id | `resolveDrop(9999, 102, {...})` | `null` ✅ |
| 존재하지 않는 `over` id | `resolveDrop(101, 9999, {...})` | `null` ✅ |
| 깨진 컨테이너 id(`day:abc`) | `resolveDrop(101, 'day:abc', {...})` | `null` ✅ |
| 데스크톱 형태(단일 Day 맵) 재정렬 | `resolveDrop(101, 103, {1: items})` | 전체 맵과 동일 결과 ✅ |

**이슈 없음.** senior-dev의 14케이스 주장을 신뢰할 수 있는 수준으로 별도 확인됨.

### 3. 모바일 시각 레이어 검증 (코드 리뷰)

- `data-drop-target` 배선(`DayEditorMobile.tsx:101-104, 164`): `handleDragOver`가 `resolveDrop()` 결과의
  `kind === 'move'`일 때만 `toDayId`를 `dropTargetDayId`에 세팅하고, `<section data-drop-target={dropTargetDayId === day.id ? 'true' : undefined}>`로
  일치하는 Day에만 부여. `resolveDrop()`은 `fromDayId !== toDayId`일 때만 `'move'`를 반환하므로 **드래그 중인
  항목의 원래 Day에는 구조적으로 `data-drop-target`이 붙을 수 없음** — 요청한 케이스 확인, 이상 없음.
- `DragOverlay`: `DayEditorMobile.tsx:191-195`에서 `<DndContext>...<DragOverlay>...</DragOverlay></DndContext>`로
  `DragOverlay`가 실제로 `DndContext` 자식. dnd-kit 기본 `z-index: 999`이고 지도는 `sticky top-0 z-10`
  (`DayEditorMobile.tsx:140`)이므로 오버레이가 항상 위에 뜸 — 문제 없음.
- 에러 배너 분기(`DayEditorPage.tsx:168-179`): `moveItemError && isDesktop`과 `moveItemError && !isDesktop`
  두 블록으로 완전히 분리. 데스크톱 블록은 기존 위치(`className="mb-4 rounded-md bg-red-50 ..."`)에서
  텍스트/스타일 변경 없음(diff에 없던 줄) — 데스크톱 표시 위치 그대로. 모바일 블록만 신규(`fixed inset-x-4 bottom-4 z-40`).
  이상 없음.
- `autoScroll={{ threshold: { x: 0, y: 0.3 } }}`(`DayEditorMobile.tsx:133`): dnd-kit 내장 옵션 값만 바뀐 것으로,
  드래그 시작/종료 로직, 센서, 충돌 판정 등 다른 코드 경로에 영향 없음(같은 파일 diff에서 이 한 줄 외에 로직
  변경 없음). `TouchSensor`(`useItineraryDndSensors.ts`)는 이번 세션에서 아예 수정되지 않음 — 스크롤/새로고침
  제스처 관련 부작용 없음(코드 리뷰 기준, 실기 미확인은 원 로그의 "사용자 확인/조치 필요"에 이미 명시돼 있음).

**이슈 없음.**

### 4. API 호출 관점

`DayEditorMobile.tsx:106-118`의 `handleDragEnd`: `resolveDrop()` 결과가 없으면(`!result`) 즉시 `return`(API 호출 없음).
`kind === 'reorder'`면 `onReorderItems`만 호출 후 `return` — `move` 분기로 내려가지 않음. `kind === 'move'`면
`onMoveItem(...)` 한 번만 호출. 분기 구조상 `reorder`와 `move`가 같은 드롭에서 동시에 발생할 수 없고(하나의
`if/return` + 그 아래 단일 `move` 호출), `reorder` 호출 뒤 `move`까지 이어서 실행되는 경로도 없음. **불필요한
중복 호출/이중 호출 없음.**

### 5. 빌드/린트 재실행

- `npm run build` (`tsc -b && vite build`) — 통과, 타입 에러 없음.
- `npm run lint` (`oxlint`) — 기존 경고 3건(`AuthContext.tsx` react/only-export-components,
  `MapView.tsx` react-hooks/exhaustive-deps 2건)만 남고 신규 경고 없음. senior-dev/ui-dev 로그의 주장과 일치.

### 종합 (심각도별)

- **치명적**: 없음.
- **보통**: 없음.
- **사소**: ADR-0005 결정 2 "데스크톱 DndContext가 변경 전과 트리 모양이 동일하다"는 주장이 **선택한 Day의
  항목이 0개인 엣지 케이스**에서는 정확하지 않음(위 1번 항목 참고) — 동작에는 영향 없고 문서 표현의 정확성
  문제이므로 지금 코드를 고칠 필요는 없다고 판단. 다만 다음에 ADR-0005를 참고할 사람을 위해 이 엣지 케이스를
  ADR에 각주로 남기거나, 원한다면 `DayEditorDesktop.tsx:66`의 조건에 `items.length > 0 &&`를 추가해 문서와
  실제 동작을 완전히 일치시키는 것도 선택지(둘 다 선택 사항, 필수 수정 아님).

이 QA 라운드에서는 **재작업 필요 이슈가 없음** — 다음 단계(실기 터치 확인)로 넘어가도 됨.

### 사용자 확인/조치 필요

- [ ] (신규 이슈 아님) 원 로그에 이미 있던 실기(안드로이드 크롬) 확인 항목 4가지는 코드 리뷰로 대체 불가 —
      그대로 유효.
- [ ] ADR-0005의 빈 Day 탭 엣지 케이스 문구는 선택적 정리 대상(치명적이지 않아 이번 라운드에서 재위임하지 않음).
- [x] ~~**git 커밋 안 함**~~ → 아래 배포 섹션에서 커밋(`03e515d`) + push 완료.

## 배포 — 커밋 + push (이어서, 같은 날)

담당: 오케스트레이터(사용자 요청). "모바일 Day 간 드래그 앤 드롭" 기능(senior-dev 구조 설계 + ui-dev 시각
구현) 전체를 커밋 `03e515d` "Support dragging itinerary items to another day on mobile"로 묶어
`git push origin main` 완료(`1df812a..03e515d`). 백엔드/DB 변경이 없어 운영 Neon DB에 별도로 할 일 없음 —
Render/Vercel 자동 재배포만으로 반영됨.

### 사용자 확인/조치 필요
- [ ] 배포 완료 후 실제 사이트에서(특히 모바일): 다른 Day로 드래그 시 하이라이트, 지도 위로 끌어도 카드가
      계속 보이는지, 화면 끝에서 자동 스크롤, 하단 에러 배너 표시/닫힘. PC에서는 기존 드래그 정렬·
      `MoveItemControl` 드롭다운 회귀 여부도 한 번 확인 권장.
- [ ] 상한값(체크리스트 100개/200자), 409/422 에러 문구 한글화(Backlog Tier 2) — 계속 열려있음.

## 여행 목록 생성 폼 접기/펼치기 + 일러스트 (ui-dev)

담당: ui-dev (오케스트레이터 위임). 사용자 요청: "여행목록의 맨 윗부분 여행이름/목적지/시작일/종료일 등
입력 필드는 '여행일정 추가' 버튼을 누르면 보이게 하자. 그 밑에 설명도 같이." + "그 공간에 작고 예쁜
여행 관련 그림을 하나 넣어주세요."

### 변경 내용

- `frontend/src/routes/TripListPage.tsx` — 상단에 항상 노출돼 있던 생성 `<form>`을 `showCreateForm`
  boolean state(기본 `false`, 접힘)로 감쌌다. 접힌 상태에서는 "+ 여행일정 추가" 버튼만 보이고, 누르면
  기존 폼(이름/목적지/시작일/종료일 입력 + 안내 문구 + 검증 에러 표시)이 그대로 펼쳐진다.
  - 접기 방법 2가지 제공: ① 펼친 폼 우측 상단의 "접기 ▲" 토글 버튼(`TripChecklist.tsx`의 기존
    펼치기 패턴과 동일한 스타일), ② 날짜 입력 옆의 명시적 "취소" 버튼(`TripEditForm`의 취소 버튼과
    같은 스타일: `border-slate-300 text-slate-600`).
  - 생성 성공(`createMutation.onSuccess`) 시 기존 필드 초기화 로직 뒤에 `setShowCreateForm(false)`
    한 줄만 추가해 저장 후 자동으로 접히게 했다. **`createMutation`의 mutationFn/검증/에러 처리
    로직은 전혀 건드리지 않았다** — 순수 표시 상태만 추가.
- **신규** `frontend/src/components/TravelIllustration.tsx` — 인라인 SVG 컴포넌트(여행 가방 + 점선
  비행 궤적을 그리는 종이비행기 모티프). 외부 이미지 파일/URL을 쓰지 않고 SVG path로 직접 그려
  에셋 파이프라인 없이도 오프라인에서 항상 동일하게 렌더링된다. 색상은 프로젝트 전반에서 쓰는
  `slate` 톤(`fill-slate-100` 배경 원, `stroke-slate-400`/`fill-slate-500` 라인)에 포인트로
  `fill-amber-400`(가방 손잡이의 작은 점) 하나만 사용 — 기존 페이지의 `amber-600`(경고 문구) 톤과
  같은 계열이라 색상 체계를 벗어나지 않는다.
  - 반응형 크기: 폼이 접혀 있을 때 버튼 옆에 `w-16 md:w-24`로 배치(모바일 64px, 데스크톱 96px 폭).
    `viewBox`만 지정하고 CSS `height`는 지정하지 않아 브라우저가 종횡비를 유지해 자동으로 높이를
    맞춘다(별도 `h-*` 클래스 불필요).
  - 배치: 접힌 상태 상단 카드 안에서 "+ 여행일정 추가" 버튼과 `flex items-center justify-between`으로
    나란히 배치(버튼 왼쪽, 일러스트 오른쪽) — 요청한 "폼이 접혀 있을 때 보이는 여백 공간"에 해당.
    폼이 펼쳐지면 이 일러스트는 폼으로 교체되어 보이지 않는다(같은 카드 안에서 조건부 렌더링).

### 회귀 확인

- `TripEditForm`, `OutOfRangeDaysModal`, `TripChecklist`, 삭제 버튼 등 카드 목록 관련 코드는 전혀
  수정하지 않음(diff 없음) — 이번 변경은 `TripListPage.tsx`의 최상단 생성 폼 블록(원래 82~136번째 줄)에
  한정.
- `npm run build` (`tsc -b && vite build`) 통과, 타입 에러 없음.

### 산출물(변경 파일)

- `frontend/src/routes/TripListPage.tsx` (생성 폼 접기/펼치기 state + 레이아웃)
- `frontend/src/components/TravelIllustration.tsx` (신규)
- `docs/PROJECT.md` (Backlog Tier 1에 항목 추가)
- `logs/2026-09-10.md`(본 섹션)

### 사용자 확인/조치 필요

- [ ] **브라우저 실기 확인 미수행** — 버튼 클릭 시 폼이 펼쳐지는지, "접기"/"취소" 둘 다 잘 닫히는지,
      모바일 폭(`w-16`)에서 일러스트가 버튼과 겹치지 않는지 확인 권장.
- [x] ~~**git 커밋 안 함**~~ → 아래 배포 섹션에서 커밋(`1671ee0`) + push 완료.

## 배포 — 커밋 + push (이어서, 같은 날)

담당: 오케스트레이터(사용자 요청). "여행 생성 폼 접기 + 일러스트" 변경을 커밋 `1671ee0`
"Collapse trip creation form behind a toggle button"으로 묶어 `git push origin main` 완료
(`c0143a7..1671ee0`). 프론트 전용 변경이라 백엔드/DB 조치 불필요, Render/Vercel 자동 재배포만으로 반영됨.

### 사용자 확인/조치 필요
- [ ] 배포 완료 후 실제 사이트에서: "+ 여행일정 추가" 버튼으로 폼 펼치기/접기/취소, 일러스트가 모바일에서
      버튼과 안 겹치는지 실기 확인.
