# Master Log

날짜별 작업 요약 인덱스. 상세 내용은 `logs/<날짜>.md` 참고. 30일 지난 로그는 `logs/archive/`로 이동됨(아래 안내 참고) — 링크가 깨져 보이면 그쪽도 확인.

| 날짜 | 요약 | 상세 로그 |
|---|---|---|
| 2026-09-03 | M0(백엔드/프론트엔드 스캐폴딩) + M1(DB 모델, 공유 비밀번호 인증, `/api/trips` CRUD) 완료. `venv312` PATH 충돌, CORS 포트 불일치 등 로컬 환경 이슈 디버깅. | [2026-09-03.md](2026-09-03.md) |
| 2026-09-04 | M2~M5 완료 (일정 CRUD, dnd-kit 드래그, Google Places 연동, 구글맵+트리플 스타일 UX 통합). 브라우저 지도 렌더링 확인 필요. | [2026-09-04.md](2026-09-04.md) |
| 2026-09-06 | **초기 개발안(M0~M6) 완성.** Neon/Render/Vercel 배포, 실제 Map ID, 예산 알림, 실기기(PC+안드로이드/크롬) 테스트 통과. SPA 새로고침 404·모바일 한글 입력 깨짐 발견/수정. 삼성 인터넷 이슈는 미해결 보류. | [2026-09-06.md](2026-09-06.md) |
| 2026-09-07 | 사용자 관점 기능 백로그 정리(`docs/PROJECT.md`에 Tier 1/2/3 + 인프라 안전장치 신설). 코드 확인으로 삭제/날짜자동생성/예산 필드의 기존 구현 여부 확정. 코드 변경 없음. 이후 같은 날 로그 구조를 폴더(`logs/<날짜>/log.md`)에서 평면 파일(`logs/<날짜>.md`)로 변경. 이어서 **M7 착수** — "여행 생성 시 날짜 범위 → Day 자동 생성" 백엔드 구현(`POST /api/trips`가 단일 트랜잭션으로 Day까지 생성) 및 curl 검증 완료. 이어서 **M7 Tier 1 프론트 3건 완료** — `TripListPage.tsx`에 삭제 버튼(confirm)·인라인 수정 폼(이름/목적지/기간, Day 비동기화 안내)·생성 폼 날짜 범위 입력(둘 다 입력 강제, 422 에러 메시지 표시) 추가, `api/trips.ts`에 `updateTrip` 신설 및 `createTrip` 반환 타입을 `TripDetail`로 수정. 빌드 통과, curl로 API 계약 일치 확인(브라우저 수동 클릭 테스트는 미완료). 이어서 **qa-reviewer 지적 수정** — `deleteMutation`에 `onError` 핸들러 누락(삭제 실패 시 무피드백) 발견/수정, 기존 `extractErrorMessage()` 재사용해 해당 항목 근처에 에러 표시. 빌드 통과, 백엔드 임시 기동 후 401/404 실패 케이스를 API 레벨로 재현·확인. 이어서 **qa-reviewer 지적 수정(백엔드)** — `PATCH /api/trips/{id}`에 날짜 범위 검증이 없어 역전·366일 기간이 200으로 통과되던 문제 해결. 검증 규칙을 `validate_date_range()` 순수 함수로 추출해 POST(스키마)/PATCH(라우터)가 공유하고, PATCH는 DB 기존 값과 병합 후 검증 → 422. curl 12케이스(회귀 포함) 통과. | [2026-09-07.md](2026-09-07.md) |
| 2026-09-08 | **M7 Tier 1 프론트 2건 완료(ui-dev)** — (1) Day 탭 자동 라벨링: `label` 없는 Day에 `Day{순번} - MM/DD(요일)` 자동 표시(`DayEditorPage.tsx`), 사용자 지정 `label`은 그대로 우선. (2) 일정 제목 인라인 수정: `api/items.ts`에 `updateItem` 추가, `ItineraryItemCard.tsx`에 클릭/연필 아이콘 → 인라인 편집 → Enter/저장 UI 추가, 빈 문자열 저장 차단. 둘 다 백엔드 변경 없음(기존 API 그대로 사용). 빌드 통과, Node로 요일 계산 검증, 백엔드 임시 기동해 curl로 API 계약 확인(브라우저 수동 클릭은 미완료). 이어서 **qa-reviewer 지적 수정(junior-dev)** — `ItineraryItemCreate`/`ItineraryItemUpdate`에 `title` 빈 값/공백 검증 누락 발견/수정. `field_validator`로 trim 후 빈 문자열 거부(422), `ItineraryItemUpdate`는 `title` 미포함(None)은 통과시키되 명시적 빈 값만 거부. trim 정규화도 함께 적용. curl 7케이스(회귀 포함) 검증 완료. 이어서 **qa-reviewer 재검증 지적 수정(junior-dev)** — 위 수정의 `if value is None: return value` 조기 리턴이 "필드 생략"과 "명시적 `{"title": null}` 전송"을 구분 못해 후자도 통과시켜 DB `NOT NULL` 위반으로 500이 나던 버그 발견/수정. None도 빈 문자열과 동일하게 422로 거부하도록 통일. TestClient 5케이스(회귀 포함) 검증 완료. 이어서 **모바일 연속 스크롤 일정 뷰(트리플 스타일) 설계 확정(senior-dev, 코드 변경 없음)** — 데이터는 백엔드 신설 엔드포인트 대신 `useQueries` Day별 병렬 조회(`useTripItems` 훅으로 캡슐화)로 결정, 데스크톱/모바일은 Google Maps 이중 마운트(Map load 이중 과금) 회피를 위해 CSS가 아닌 JS 미디어쿼리로 택일 렌더, 활성 Day 추적은 고정 블록 높이를 `ResizeObserver`로 측정해 `rootMargin`을 잡는 IntersectionObserver 스크롤 스파이. Day 라벨은 `Day1 9/10(목)`으로 확정. ui-dev용 구현 스펙 작성 완료(구현은 다음 세션). 이어서 **모바일 연속 스크롤 일정 뷰 구현 완료(ui-dev)** — 스펙 그대로 신규 파일 7개(`lib/dayLabel.ts` 등) 추가하고 `DayEditorPage.tsx`를 컨테이너로 리팩터링, 데스크톱은 동작 변화 없이 유지·모바일은 지도 상단 고정+연속 스크롤+IntersectionObserver 스크롤 스파이로 전환. `MapView`/`ItineraryList`/`ItineraryItemCard`/`AddItemModal`/API/백엔드는 무변경. 빌드 통과, curl+Node로 라벨 포맷/모듈 트랜스폼 검증(브라우저 자동화 도구 없어 실제 스크롤·핀 전환은 미확인, 사용자 확인 필요). 이어서 **qa-reviewer 지적 수정(ui-dev)** — `DayEditorDesktop.tsx`가 로딩 중에도 `<ItineraryList>`에 빈 배열을 넘겨 "불러오는 중..."과 "등록된 일정이 없습니다"가 동시에 잠깐 보이는 false empty state 회귀 발견/수정. `!itemsLoading &&`로 게이팅해 원본(`{items && (...)}`undefined 방어)과 동일한 동작으로 복구, 지도는 원본처럼 로딩 중에도 그대로 렌더. `DayEditorMobile.tsx`는 이미 전체 Day 섹션이 `itemsLoading`으로 게이팅돼있어 동일 회귀 없음(코드 변경 없음). 빌드 통과, 코드 추적으로 회귀 없음 확인. qa 발견 사소 이슈 3건(`isError` 미사용, ADR `isPending`/코드 `isLoading` 불일치, `registerSection` 언마운트 미처리)은 `docs/PROJECT.md` Tier 2에 백로그만 기록(미수정). | [2026-09-08.md](2026-09-08.md) |
| 2026-09-09 | **M7 Tier 1 마지막 항목 "여행 기간(PATCH) 수정 시 Day 동기화" 백엔드 완료(senior-dev)** — 설계 결정을 이 프로젝트 첫 ADR([`docs/adr/0001-patch-trip-date-day-sync.md`](../docs/adr/0001-patch-trip-date-day-sync.md), `docs/adr/` 신설)로 기록. **PATCH는 "추가만 하고 절대 삭제하지 않는" 비파괴 연산**으로 고정하고(Day→ItineraryItem이 CASCADE라 자동 삭제는 곧 일정 손실), 범위 밖 Day는 응답 `out_of_range_days`(+`item_count`)로 보고만 해서 프론트가 확인받은 뒤 **기존 `DELETE /api/days/{id}`**로 지우는 2단계 방식 채택(새 엔드포인트 0개). 후보 `force=true`(파괴적 동작을 일반 수정에 묶어 클라 실수 한 줄이 곧 손실, 경고하려면 어차피 사전 조회 필요)와 "남겨두고 표시만"(정리 수단 없음)은 기각. 구현: `diff_days_for_range()`/`DayRangeDiff`/`MAX_TRIP_TOTAL_DAYS`(=180, 기간 이동 PATCH 반복 시 Day 무한 누적 방지) 추가(`services/trip_days.py`), `TripUpdateResult`/`OutOfRangeDayRead` 추가(`schemas.py`, `TripRead` 상위집합이라 기존 프론트 호환), `update_trip`에 동기화 로직(기존 `validate_date_range()` 흐름은 그대로 두고 뒤에 이어붙임, 요청에 날짜 키가 있을 때만 동기화). DB 스키마 변경 없어 마이그레이션 불필요. TestClient 8시나리오 46체크 통과(임시 SQLite로 실행, `dev.db` 무변경). **같은 날 프론트 완료(ui-dev)** — `types/models.ts`에 `OutOfRangeDay`/`TripUpdateResult` 추가, `api/trips.ts`의 `updateTrip()` 반환 타입 확장. 신규 `components/OutOfRangeDaysModal.tsx`(범위 밖 Day + `item_count` 경고 표시, "삭제"는 대상 Day마다 기존 `deleteDay()`를 `Promise.allSettled`로 병렬 호출해 부분 실패 허용·실패 항목만 에러 표시, "유지"는 삭제 없이 닫기)와 `lib/errors.ts`(신규, 기존 `extractErrorMessage()` 공유 위치로 이동) 추가. `TripListPage.tsx`의 `TripEditForm`이 저장 성공 시 범위 밖 Day가 있으면 이 모달을, 추가된 Day만 있으면 "N일이 추가되었습니다" 인라인 안내를 띄우도록 변경, 안내 문구도 새 동작에 맞게 갱신. `DayEditorMobile.tsx`/`lib/dayLabel.ts`는 무변경(스코프 밖). `npm run build` 통과, 브라우저 수동 클릭 테스트는 미수행. **M7 Tier 1 백로그 전 항목 백엔드+프론트 완료.** 커밋 안 함(사용자 확인 필요 항목은 상세 로그 참고). **이어서 qa-reviewer 검증** — API 레벨 재현 9시나리오 전부 기대대로 통과, 치명적 이슈 없음. 보통 1건(`Day(trip_id, date)`에 DB 유니크 제약이 없어 동시 PATCH 시 중복 Day 생성 가능 — 2인 공유 앱 전제와 맞물리는 레이스 컨디션, 미수정 리포트만), 사소 1건(`OutOfRangeDaysModal`이 삭제 전부 실패해도 캐시 무효화 — 낭비성 refetch, 기능 문제 없음). 캐시 무효화 범위(`DayEditorPage.tsx`)는 회귀 없음 확인. `npm run build` 재통과. **이어서 [보통] 이슈 수정(senior-dev)** — `Day`에 `(trip_id, date)` 유니크 제약(`uq_day_trip_id_date`) + Alembic `73a697fa5fee` 추가(중복 사전 확인 가드 포함, 중복 있으면 일부러 실패). 위반은 `update_trip`(트랜잭션 전체 롤백)·`create_day`·`update_day`에서 409로 변환하고 **자동 재시도는 안 함**(상대가 기간까지 바꿨을 수 있어 재시도가 곧 last-write-wins). `MAX_TRIP_TOTAL_DAYS` 동시 우회는 락 대신 알려진 한계로 문서화(SQLite에서 `FOR UPDATE`가 무시돼 검증 불가, 유니크 제약 덕에 최대 270에서 멈추고 자가 수복). 근거는 후속 [ADR-0002](../docs/adr/0002-day-unique-constraint-and-conflict-handling.md). TestClient 58체크 통과(기존 9시나리오 회귀 포함), `dev.db` 마이그레이션 적용/롤백 왕복 + 데이터 무손실 확인. 운영 Neon에 `alembic upgrade head` 필요. **이어서 qa-reviewer 재검증에서 나온 [보통] 이슈 수정(junior-dev)** — `DayEditorPage.tsx`의 "날짜 추가" 폼이 409 등 에러를 완전히 무시하던 문제를 `onError`+`extractErrorMessage()`로 해결, ADR-0002/로그의 "이 API를 부르는 화면이 없다"는 부정확한 서술도 정정. 빌드 통과. **이어서 배포** — 운영 Neon DB에 `alembic upgrade head` 시도 시 트립 6번("테스트")에서 실제 중복 Day 발견(설계대로 안전하게 실패), 일정 데이터 병합(Day 10→Day 6 이동) 후 재실행 성공. 커밋(`93fde66`)+`git push origin main` 완료, Render/Vercel 자동 재배포 시작. **이어서 M7 Tier 1 "일정 메모/체크리스트" 백엔드 완료(senior-dev)** — 사용자 확인으로 범위를 "여행(Trip) 단위 체크리스트(항목별 체크박스+삭제)"로 확정. 새 모델 `ChecklistItem`(`trip_id` FK CASCADE/`text`/`is_checked`/`created_at`) + 마이그레이션 `d6eea980ecbe`, `ChecklistItemCreate/Update/Read`(`schemas.py`), 신규 라우터 `app/routers/checklist.py`(`GET|POST /api/trips/{id}/checklist`, `PATCH|DELETE /api/checklist/{itemId}`, 세션 쿠키 보호). 설계 근거는 [ADR-0003](../docs/adr/0003-trip-checklist-model.md) — **정렬 컬럼을 일부러 두지 않고 생성 순서(`id`) 고정**(준비물은 순서에 의미가 없고, 나중에 필요하면 `position DEFAULT 0` 컬럼 하나로 백필 없이 확장 가능 → 지금 안 넣는 쪽이 되돌리기 싸다), **토글은 전용 엔드포인트가 아닌 멱등한 일반 PATCH**(`toggle`은 재시도/더블탭이 상태 반전 2회가 됨), 트립당 100개+텍스트 200자 상한(개수만 있으면 무의미해서 세트로). `ItineraryItemUpdate.title`에서 겪은 "필드 생략 vs 명시적 null" 버그를 `text`·`is_checked` 양쪽에서 방어. TestClient 61체크 통과(인증 401·트립 삭제 CASCADE·개수 상한 포함), 마이그레이션 왕복 확인, `dev.db` 데이터 무변경. **이어서 같은 날 프론트 완료(ui-dev)** — `types/models.ts`에 `ChecklistItem`, 신규 `api/checklist.ts`(4개 함수, `api/days.ts` 스타일), 신규 `components/TripChecklist.tsx`를 `TripListPage.tsx`의 각 여행 카드 안에 배치(모달 아닌 인라인 펼치기 섹션). 접힌 상태에서는 `enabled: isOpen`으로 API를 호출하지 않되, React Query가 `enabled`와 무관하게 같은 쿼리 키의 캐시를 반환하는 특성을 이용해 한 번 펼친 뒤로는 캐시로 "준비물 N/M" 배지를 유지(추가 fetch 없음, 별도 상태 복제도 불필요). 체크 토글은 낙관적 업데이트+롤백, 추가/삭제는 응답으로 캐시 갱신, 빈 값/200자 초과는 클라이언트에서 선차단. `npm run build` 통과. **M7 Tier 1 "일정 메모/체크리스트" 백엔드+프론트 모두 완료.** 운영 Neon `alembic upgrade head`(`d6eea980ecbe`) 필요, 브라우저 실기 확인 미수행, 커밋 안 함(사용자 확인 필요, `logs/2026-09-09.md` 참고). **이어서 qa-reviewer 1차 검증** — 독립 TestClient(인메모리 SQLite) 27체크 전부 통과(텍스트 검증/`is_checked` null 방어/100개 상한 경계/트립 간 격리/401/CASCADE/부분 업데이트), 프론트는 코드 리뷰로 낙관적 업데이트 롤백·`enabled: isOpen`·캐시 키 격리·클라이언트 200자 경계·`TripListPage.tsx` 무회귀(`git diff` 2줄) 확인, `npm run build` 재통과. 치명적/보통 이슈 없음, 사소 1건(카드를 연 직후 초기 GET 완료 전에 항목을 추가하면 뒤늦은 GET 응답이 화면에서 일시적으로 덮어씀 — 서버 데이터 손실 없음, 미수정 리포트만). **이어서 배포** — 운영 Neon DB에 `alembic upgrade head`(`d6eea980ecbe`) 성공(새 빈 테이블 생성뿐이라 무리 없이 적용), 커밋(`7da2288`)+`git push origin main` 완료, Render/Vercel 자동 재배포 시작. **M7 Tier 1 백로그 전 항목 완료.** **이어서 신규 요청 "일정을 다른 Day로 옮기기" 백엔드 완료(senior-dev, 실제 작업일 2026-09-10 — 09-09 세션의 연속이라 같은 로그 파일에 기록)** — 앞선 세션이 API 레이트리밋으로 중단돼 워킹트리에 남아 있던 미완성 변경(`routers/items.py`, `schemas.py`)을 `git diff`로 검토, 설계가 기존 패턴(`reorder`의 동사형 POST, `delete_item`의 삭제 후 재번호)과 일관돼 **갈아엎지 않고 이어서 완성**. 새 엔드포인트 `POST /api/items/{item_id}/move`(바디 `{"target_day_id": int}`) — 목적지 Day **맨 뒤**에 붙이고 원본 Day는 `position`을 0..N-1로 재번호(이동+재번호를 한 번의 commit으로), **다른 트립의 Day는 422 거부**(존재하지 않는 404와 구분 — 막지 않으면 프론트 캐시 키가 트립 단위라 눈에 안 띄는 조용한 데이터 오염), 없는 Day는 404, 같은 Day는 no-op 200(멱등). 삽입 위치(`target_position`)는 **일부러 받지 않음**(재배치 로직이 기존 `reorder`와 중복되고 그 값을 채울 UI가 데스크톱/모바일 어디에도 없음, 나중에 바디 필드로 하위호환 추가 가능). 근거는 [ADR-0004](../docs/adr/0004-move-item-between-days.md). **검증 중 [보통] autoflush 버그 발견·수정** — `item.day_id`를 먼저 바꾼 뒤 최대 position을 SELECT하면 autoflush가 그 변경을 먼저 밀어넣어 자기 자신을 최댓값으로 집어삼켜 position이 "맨 뒤"가 아니라 "옛 position + 1"이 됨(빈 Day로 옮기면 0이 아니라 3 등). 평범한 케이스는 우연히 맞아 눈으로는 안 보였고 검증 스크립트 3건 실패로 드러남. 읽기를 전부 끝낸 뒤 수정하도록 순서 변경해 해결. TestClient 32체크 전부 통과(임시 SQLite, `dev.db` 무접근). **DB 스키마 변경/마이그레이션 없음 → 운영 Neon 조치 불필요**, 백엔드 배포만으로 반영. 커밋 안 함(프론트 완료 후 일괄 예정). **이어서 같은 날 프론트 완료(ui-dev, 실제 작업일 2026-09-10 — 09-09 세션의 연속)** — `api/items.ts`에 `moveItem(itemId, targetDayId)` 추가, 신규 공유 컴포넌트 `components/MoveItemControl.tsx`(Day 목록/현재 Day 비활성화/선택 로직 공유, 뜨는 방식만 `variant`로 분기 — 데스크톱은 드롭다운, 모바일은 연속 스크롤에 묻히지 않는 하단 시트). `ItineraryItemCard.tsx`/`ItineraryList.tsx`는 `days`/`moveVariant`/`onMove` props를 관통시키는 방식으로 확장(기존 제목 인라인 편집/삭제/dnd-kit 드래그 정렬 무변경). `DayEditorPage.tsx`에 `moveItemMutation` 추가 — **낙관적 업데이트 없음**(senior-dev 권장, 원본/목적지 두 Day 캐시와 서버 position 재번호를 동시에 흉내 내야 해 복잡도 대비 이득 적음), 성공 시 원본·목적지 두 `['days', dayId, 'items']` 캐시 모두 invalidate, 실패 시 `extractErrorMessage()`로 상단에 에러 표시. 모바일은 이동 성공 후 이미 있던 Day 점프 칩의 `jumpToDay()`를 재사용해 목적지 Day 섹션으로 자동 스크롤. 드래그 앤 드롭은 사용하지 않음(ADR-0004에서 이미 기각). `npm run build` 통과. **M7 Tier 1 "일정을 다른 Day로 옮기기" 백엔드+프론트 모두 완료.** 브라우저 실기 확인·git 커밋은 미수행이나, **이어서 qa-reviewer 1차 검증 완료** — autoflush 버그 재발 여부를 핵심으로 새 TestClient 스크립트(27체크: 빈/비어있지 않은 Day로 이동 시 position 정확성, 원본 재번호, 다른 트립 422+원자성, 같은 Day no-op, 404/401, 이동 후 reorder 회귀)를 독립 재작성해 전부 통과 확인, 프론트는 `git diff`로 기존 로직(제목 편집/삭제/드래그 정렬) 무변경 확인 + 원본·목적지 두 캐시 invalidate/현재 Day 비활성화 표시를 코드로 확인, `npm run build` 재통과. **치명적/보통/사소 이슈 없음 — 이 기능의 첫 QA 라운드가 클린 패스.** **이어서 배포(2026-09-10)** — 커밋(`12254e9`)+`git push origin main` 완료, 스키마 변경 없어 운영 DB 조치 불필요, Render/Vercel 자동 재배포만으로 반영. | [2026-09-09.md](2026-09-09.md) |
| 2026-09-10 | **모바일 Day 간 드래그 앤 드롭 설계 확정 + 구조 변경(senior-dev)** — 사용자 후속 요청("PC는 드롭다운 그대로, 모바일은 드래그로도 옮기게")에 따라 ADR-0004가 기각했던 Day 간 드래그를 **모바일에만** 도입. 코드 확인 결과 `DndContext`가 `ItineraryList` 안에 있어 모바일에서 Day 개수만큼 독립 컨텍스트가 생기고 있었고(=Day 간 드래그 원천 불가), 이를 상위 뷰로 올리는 게 전제조건이었다. **모바일은 Day1~DayN 전체를 감싸는 단일 DndContext, 데스크톱은 활성 Day 하나만 감싸는 DndContext(트리 모양이 변경 전과 동일 → 회귀 없음)**. 드롭 해석은 신규 순수 함수 `lib/dndDrop.ts`의 `resolveDrop()`(reorder/move/null) 단일 소스로 통일, 센서는 `hooks/useItineraryDndSensors.ts`로 추출(M3 튜닝값 무변경). **빈 Day에만** 컨테이너 droppable(`day:<id>`)을 등록해 "옮겨올 수 없는 Day"를 없애고, 그 덕에 컨테이너↔항목 경합이 없어 커스텀 collision 캐스케이드 없이 기존 `closestCenter` 유지. 다른 Day로 드롭 = 그 Day **맨 뒤** 삽입(기존 `POST /api/items/{id}/move` 그대로 → **백엔드/DB/API 변경 없음**), `target_position`은 추가하지 않고 삽입선 대신 목적지 Day 섹션 하이라이트로 표시(정확한 위치 삽입은 드래그 중 로컬 상태 사본이 필요해져 이미 잘 도는 같은-Day 정렬의 회귀 위험을 사기 때문). 자동 스크롤은 dnd-kit 내장(기본 활성) 사용 — 별도 구현 없음. `MoveItemControl`(하단 시트)은 먼 Day 이동·드래그 실패 대비로 **유지**. 근거는 [ADR-0005](../docs/adr/0005-mobile-cross-day-drag-and-drop.md). 검증: `npm run build` 통과, oxlint 신규 경고 없음, `resolveDrop()` 14케이스(같은-Day 정렬 회귀 6건 포함) 통과. 시각 효과(섹션 하이라이트/`DragOverlay`/이동 실패 배너 위치)와 실기 터치 확인은 ui-dev 후속 — 스펙은 상세 로그에 정리. 커밋 안 함. **이어서 시각/UI 구현 완료(ui-dev, 같은 날)** — `DayEditorMobile.tsx`의 드롭 대상 `<section>`에 `data-[drop-target]:` variant로 배경/링 하이라이트 추가(삽입선은 스펙대로 그리지 않음), `<DragOverlay>` + 카드 스타일을 재사용한 프리젠테이션 전용 `ItineraryItemDragPreview` 추가(기본 z-index 999로 상단 sticky 지도 위에 뜸, 지도에 가려지던 문제 해결), `autoScroll={{ threshold: { x: 0, y: 0.3 } }}`로 세로 자동 스크롤 발동 지점을 지도 아래로 조정(dnd-kit 내장 파라미터 조정만). `DayEditorPage.tsx`의 `moveItemError` 배너를 `isDesktop` 분기로 나눠 모바일만 `fixed inset-x-4 bottom-4`(탭하면 닫힘)로 이동, 데스크톱은 기존 위치·mutation/상태 로직 무변경. `TouchSensor`/`lib/dndDrop.ts`/`DndContext` 배치/`MoveItemControl.tsx`는 전혀 손대지 않음. `npm run build`/`npm run lint` 통과(신규 경고 없음). **M7 Tier 1 "모바일 드래그로 Day 옮기기" 완료** — 실기 터치 확인만 남음. **이어서 qa-reviewer 검증** — 데스크톱 회귀 없음(센서/충돌판정/재정렬 호출 방식 `git show` 대조로 확인), `resolveDrop()` 13케이스 직접 재현 통과, 시각 레이어/API 호출 전부 정상. 치명적/보통 이슈 없음, 사소 1건(ADR의 "DndContext 트리 완전 동일" 주장이 항목 0개 Day 엣지케이스에서 엄밀친 않음 — 동작 영향 없어 문서 정확성 문제로만 기록). **이어서 배포** — 커밋(`03e515d`)+`git push origin main` 완료, 스키마 변경 없어 운영 DB 조치 불필요. **이어서 사용자 요청 "여행 목록 상단 생성 폼 접기/펼치기 + 작은 일러스트"(ui-dev)** — `TripListPage.tsx` 생성 폼을 기본 접힘 + "+ 여행일정 추가" 버튼으로 펼치기/접기 토글로 변경(카드 목록/기존 mutation 로직 무변경), 접힌 자리에 신규 인라인 SVG `components/TravelIllustration.tsx`(여행 가방+종이비행기, `slate` 톤, 반응형 `w-16 md:w-24`) 배치. `npm run build` 통과, 백엔드/DB 변경 없음. **이어서 배포** — 커밋(`1671ee0`)+`git push origin main` 완료, 프론트 전용이라 운영 DB 조치 불필요. **이어서 `DayEditorPage.tsx`의 "날짜 추가" 폼도 동일 패턴으로 접기(ui-dev)** — `showCreateDayForm` state로 기본 접힘, "+ 날짜 추가" 버튼으로 펼침, 기존 `createDayMutation`/에러 처리 무변경. 여행가방 일러스트 재사용 대신 이 페이지 성격(Day 탭+지도)에 맞춰 신규 `components/CalendarIllustration.tsx`(달력+위치 핀) 제작. `npm run build` 통과. **이어서 배포** — 커밋(`745d640`)+push 완료, 운영 DB 조치 불필요. **이어서 M7 Tier 1 마지막 항목 "예산/지출 추적 + 정산(더치페이)" 백엔드 완료(senior-dev)** — 참가자 관리 기능 없이(희경/재승 2명 고정) `ItineraryItem.paid_by` 컬럼 + 마이그레이션 `093074f849bc`(운영 Neon 미적용), 순수 계산 모듈 `services/settlement.py` + 읽기 전용 `GET /api/trips/{id}/settlement`(참가자별 합계/총합/1인당/누가 누구에게 얼마 + 미지정·타통화 지출 보고, 환율 변환 없음). 근거 [ADR-0006](../docs/adr/0006-expense-payer-and-settlement.md), TestClient 86개 체크 통과. 프론트는 ui-dev 후속, git 커밋 안 함. **(이어서 2026-09-11)** 사용자 후속 요청("닉네임을 마스터 환경설정에서 변경 가능하게 + 메인페이지에 노출")으로 **결제자 저장 방식을 재설계**(senior-dev) — 이름 변경이 드문 배포 이벤트가 아니라 런타임 기능이 되면서 ADR-0006의 전제가 사라졌다. `paid_by`에 표시 이름 대신 **고정 슬롯 키**(`participant_1`/`participant_2`)를 저장하고 이름은 새 싱글턴 테이블 `app_settings`에서 읽는 방식으로 교체(마이그레이션 `0e8fd49e8ad8`, 신규 API `GET|PATCH /api/settings`). "표시 이름 유지 + 이름 변경 시 지출 일괄 UPDATE" 안은 **재작성을 놓친 행이 에러 없이 정산에서 사라지고 상대 이름으로 바꾸면 복구 불가능하게 병합된다**는 이유로 기각. 근거는 [ADR-0007](../docs/adr/0007-runtime-participant-names.md)(ADR-0006 상단에 후속 포인터 추가). TestClient+순수 함수 79개 체크 통과. 마스터 환경설정 화면은 ui-dev 후속. | [2026-09-10.md](2026-09-10.md) |
| 2026-09-11 | **마스터 환경설정 화면 + 비용/결제자 입력 + 정산 결과 표시 — 프론트 완료(ui-dev)** — 이 항목으로 **M7 Tier 1 "예산/지출 추적 + 정산(더치페이)"가 백엔드+프론트 모두 완료**되며 M7 Tier 1 백로그가 전부 소진됨. `TripListPage.tsx` 헤더에 톱니바퀴 버튼 → `AppSettingsModal.tsx`(신규, `OutOfRangeDaysModal`과 같은 모달 패턴 — 카드가 아닌 페이지 전역 기능이라 모달로 배치)에서 참가자 2명 이름 수정, 바뀐 슬롯만 PATCH, 성공 시 `['settings']`만 invalidate(설계 의도대로 결제자 선택박스/일정 카드/정산 화면 전체에 자동 전파). `AddItemModal.tsx`(직접 입력 탭)와 `ItineraryItemCard.tsx`(항상 노출되는 인라인 금액 input + 결제자 select)에 비용/결제자 입력 추가, 서버로는 이름이 아니라 **슬롯 키**를 전송. 각 여행 카드에 `TripChecklist`와 같은 `enabled: isOpen` 패턴의 "정산" 펼치기 섹션(`TripSettlement.tsx`, 신규)을 추가해 `transfer: null`/결제자 미지정 건수/통화 혼합 경고를 모두 안내, 참가자 표시 이름은 정산 응답보다 `['settings']` 캐시를 우선해 이름 변경이 즉시 반영되게 함. 신규 `api/settings.ts`/`api/settlement.ts`, `types/models.ts`에 `Participant`/`AppSettings`/`SettlementResult` 등 추가. `TripEditForm`/`OutOfRangeDaysModal`/`TripChecklist`/드래그 정렬·이동/일정 제목 인라인 수정은 코드 diff 없음(회귀 없음). `npm run build`/`npm run lint` 통과(신규 경고 없음). 남은 것: 운영 Neon `alembic upgrade head`(3개 밀림), 브라우저 실기 확인. 커밋 안 함. **이어서 qa-reviewer 검증** — 백엔드 API/마이그레이션/프론트 코드리뷰 전부 통과, 치명적 이슈 없음, 보통 1건(`AddItemModal.tsx` 비용 저장 실패 무피드백 + 입력창 조기 초기화). **이어서 [보통] 이슈 수정(ui-dev)** — `AddItemModal.tsx`에 `onError`+`extractErrorMessage()` 에러 표시 추가, 성공 시에만 입력창 초기화하도록 수정. `ItineraryItemCard.tsx`의 비용/결제자 인라인 편집도 `onUpdateItem` 콜백에 선택적 `onError` 파라미터를 추가해(제목 편집 경로는 무변경) 카드 근처에 에러 표시. `npm run build` 통과, 커밋 안 함. **이어서 qa-reviewer 재검증** — 콜백 체인 끝까지 추적 확인(끊긴 곳 없음), 제목 편집 회귀 없음, 백엔드 422 응답 형식이 `extractErrorMessage()`와 일치 확인. 치명적/보통 이슈 없음 — 재작업 필요 사항 해소 확인. **M7 Tier 1 백로그 전 항목 완료.** **이어서 배포(2026-09-11)** — 운영 Neon DB에 밀려있던 마이그레이션 3개(`093074f849bc`/`0e8fd49e8ad8`, 체크리스트 마이그레이션 `d6eea980ecbe`는 기존 적용분) `alembic upgrade head` 한 번으로 에러 없이 적용, 커밋(`1c7f4b9`)+`git push origin main` 완료. | [2026-09-10.md](2026-09-10.md) |
| 2026-09-11 | **M7 Tier 2 "캘린더 내보내기(ICS → 구글 캘린더)" 백엔드 완료(senior-dev)** — 신규 `GET /api/trips/{trip_id}/export.ics`(`app/routers/export.py`) + 순수 생성 모듈 `app/services/ics_export.py`. **DB 스키마 변경/마이그레이션 없음**(기존 필드를 읽기만 하는 순수 변환) → 운영 Neon 조치 불필요, 백엔드 배포만으로 반영. 조사 결과 **프론트에 시간 입력 UI가 없어 `start_time`/`end_time`이 전부 NULL**이고 `Trip`에 시간대 필드도 없다는 점이 설계를 결정 — 일정 1건 = VEVENT 1개(시간 없으면 종일 이벤트, 제목 앞에 그날 순번 `"1. "`), 시각은 `Z`/`TZID` 없는 floating time, UID는 `item-{id}@travel-planner-dtsjs.vercel.app` 고정(재내보내기 = 중복이 아니라 갱신), `icalendar` 의존성 없이 표준 라이브러리로 생성(CRLF/TEXT 이스케이프/75옥텟 폴딩 직접 처리 — 한글은 3옥텟이라 폴딩이 예외가 아닌 기본 상태). "Day를 한 이벤트로 묶기"는 LOCATION/좌표 손실로, "09:00부터 1시간씩 자동 배정"은 없는 정보를 지어내는 것이라 기각. 근거는 [ADR-0008](../docs/adr/0008-ics-calendar-export.md). TestClient+순수 함수 **103개 체크 통과**(임시 SQLite, `dev.db` 무접근). 다운로드 버튼 배치는 ui-dev 후속(평범한 `<a href>` 링크 한 줄, blob 불필요 — 상세 스펙은 로그 참고). 커밋 안 함. | [2026-09-11.md](2026-09-11.md) |
| 2026-09-12 | **Backlog Tier 2 기술 부채 정리 — `Trip.name`/`ItineraryItem.title` 길이 상한 추가(junior-dev, 백엔드 전용, orchestrator_pjt 위임)** — `app/schemas.py`에 pydantic 검증으로 `MAX_TRIP_NAME_LENGTH`(100자)/`MAX_ITINERARY_ITEM_TITLE_LENGTH`(150자) 추가(`ChecklistItem.text`와 동일 패턴, **DB 스키마 변경/마이그레이션 없음**). `TripUpdate.name`에 명시적 null 거부 validator도 함께 신설(`ItineraryItemUpdate.title`이 이미 겪었던 "필드 생략 vs 명시적 null" 500 버그의 잠재 재발을 방지). TestClient 18개 체크(경계값+회귀+명시적 null) 통과, 임시 SQLite 오버라이드로 `dev.db` 무접근. 프론트 항목(`useActiveDayOnScroll`)과 `MAX_TRIP_TOTAL_DAYS` 동시 우회(ADR-0002에서 이미 "수정 안 함" 결론)는 스코프 밖. 커밋 안 함. | [2026-09-12.md](2026-09-12.md) |
| 2026-09-11 | **M7 Tier 2 "캘린더 내보내기(ICS)" 프론트 완료(ui-dev)** — `TripListPage.tsx` 여행 카드의 수정/삭제 버튼 옆에 `<a href={`${API_BASE_URL}/api/trips/${id}/export.ics`}>📅 내보내기</a>` 링크 추가(스펙대로 blob 미사용, `download` 속성 없음, 새 API 함수 없음). 안내 문구는 `title` 툴팁으로. 이 항목으로 **M7 Tier 2 "캘린더 내보내기"는 백엔드+프론트 모두 완료**. `npm run build` 통과, 기존 카드 기능(준비물/정산/수정/삭제) 회귀 없음. 커밋 안 함. | [2026-09-11.md](2026-09-11.md) |
| 2026-09-11 | **M7 Tier 2 "캘린더 내보내기(ICS)" qa-reviewer 독립 재검증** — senior-dev의 "103개 체크 통과" 보고를 신뢰하지 않고 TestClient 기반 38체크를 새로 작성해 재현(이스케이프 순서/CRLF/75옥텟 폴딩+UTF-8 왕복/UID 안정성/빈 여행/시간 있는·없는 일정 혼합/GEO·위험URL 생략/Content-Disposition ASCII 안전성/트립 격리 전부 PASS). 임시 uvicorn을 별도 기동해 실제 curl로 `.ics` 수신·육안 확인(RFC 5545 최소 요건 충족), 프론트는 `git diff`로 `<a href>` 순수 링크·`download` 미사용·`API_BASE_URL` 재사용·기존 컴포넌트 무회귀 확인, `npm run build` 재통과. **치명적/보통/사소 이슈 없음.** `dev.db`는 시각 불변(무오염). 구글 캘린더 실기 가져오기 확인은 여전히 사람 몫으로 남음. **이어서 배포** — 커밋(`5ae5589`)+`git push origin main` 완료, 마이그레이션 없어 운영 DB 조치 불필요. | [2026-09-11.md](2026-09-11.md) |
| 2026-09-12 | **Backlog Tier 2 기술 부채 정리 — 프론트엔드 3건(ui-dev, orchestrator_pjt 위임, junior-dev 백엔드 작업과 별도 병행)** — ① `useTripItems`의 `isError` 미사용 문제: 신규 `errorDayIds`를 훅에 추가해 `DayEditorPage`→`DayEditorDesktop`/`DayEditorMobile`→`ItineraryList`까지 배선, Day별로 "일정을 불러오지 못했습니다"(빈 Day와 색으로 구분)를 표시. ② ADR 문구 `isPending` vs 코드 `isLoading` 불일치: 코드를 `result.isPending` 기준으로 수정(반환 필드명은 유지, 소비자 1곳 확인 후 단순 개명 수준으로 확정). ③ `useActiveDayOnScroll`의 `registerSection`이 언마운트(`el===null`)를 처리하지 않던 문제: 시그니처를 `(dayId, el)`로 바꾸고 내부 추적을 `Map<number, HTMLElement>`로 교체해 `unobserve()` 처리(현재는 도달 불가능한 코드지만 향후 Day 삭제 기능 대비). 기존 기능(드래그 정렬/이동, 인라인 편집, 정산/체크리스트 등) 전부 무변경. `npm run build`/`npm run lint` 통과(신규 경고 없음). 백엔드/DB 변경 없음, 커밋 안 함. | [2026-09-12.md](2026-09-12.md) |
| 2026-09-12 | **Backlog Tier 2 기술 부채 정리 5건 — qa-reviewer 독립 재검증(orchestrator_pjt 위임)** — junior-dev(백엔드 길이 상한 2건+null 방어)/ui-dev(프론트 3건) 보고를 신뢰하지 않고 재검증. 백엔드는 TestClient 신규 스크립트로 24체크(경계값/회귀/명시적 null 422/필드 생략 회귀 없음) 통과, `dev.db` mtime 불변으로 무오염 확인. 프론트는 `git diff`로 `errorDayIds` 배선·`hasError` 전파·색상 구분, `isLoading`→`isPending` 전환이 과거 회귀 지점(`itemsLoading` 게이팅)에 영향 없음, `useActiveDayOnScroll`의 `Map` 전환이 판정 알고리즘 무변경임을 코드로 확인, `TripEditForm` 등 무관 컴포넌트는 diff에 없어 회귀 가능성 없음. `npm run build`/`npm run lint` 재통과. **치명적/보통/사소 이슈 없음** — 재작업 불필요. **이어서 배포** — 커밋(`8429b48`)+`git push origin main` 완료, 마이그레이션 없어 운영 DB 조치 불필요. | [2026-09-12.md](2026-09-12.md) |
| 2026-09-12 | **Backlog Tier 2 "409/422 에러 응답 한국어 매핑" 완료(ui-dev, orchestrator_pjt 위임, 백엔드 무변경)** — 전수 grep 재조사로 ADR-0002 시점 18곳이던 에러 문구가 체크리스트/정산/설정/ICS/일정 이동 등 추가로 25종(정적 17+동적 상한값·필드명 포함 8)까지 늘어난 것을 확인. 신규 `frontend/src/lib/errorMessages.ts`(정적 `EXACT_MESSAGES` + 정규식 `DYNAMIC_MESSAGE_RULES`, pydantic `"Value error, "` 접두사 처리 포함)로 매핑 테이블을 분리하고, `extractErrorMessage()`가 이를 우선 조회하되 매핑이 없으면 기존과 동일하게 원문 그대로 폴백하도록만 수정(호출 규약 무변경이라 기존 12개 호출부가 자동 한글화됨). `venv312`로 로컬 백엔드를 임시 기동해 실제 curl 응답(트립 없음/날짜 역전/이름 길이 초과/Day 중복 409/reorder 불일치/참가자 이름 중복/비용 음수/결제자 오류/체크리스트 빈 값)을 캡처해 매핑 결과를 재확인, 매핑 없는 임의 문구는 원문 폴백됨도 확인. `npm run build`/`npm run lint` 통과(신규 경고 없음). Tier 2에는 이동시간 표시/여행 복제·템플릿/삼성 인터넷 이슈/PWA가 아직 남아있음. 커밋 안 함. | [2026-09-12.md](2026-09-12.md) |
