# Master Log

날짜별 작업 요약 인덱스. 상세 내용은 `logs/<날짜>.md` 참고. 30일 지난 로그는 `logs/archive/`로 이동됨(아래 안내 참고) — 링크가 깨져 보이면 그쪽도 확인.

| 날짜 | 요약 | 상세 로그 |
|---|---|---|
| 2026-09-03 | M0(백엔드/프론트엔드 스캐폴딩) + M1(DB 모델, 공유 비밀번호 인증, `/api/trips` CRUD) 완료. `venv312` PATH 충돌, CORS 포트 불일치 등 로컬 환경 이슈 디버깅. | [2026-09-03.md](2026-09-03.md) |
| 2026-09-04 | M2~M5 완료 (일정 CRUD, dnd-kit 드래그, Google Places 연동, 구글맵+트리플 스타일 UX 통합). 브라우저 지도 렌더링 확인 필요. | [2026-09-04.md](2026-09-04.md) |
| 2026-09-06 | **초기 개발안(M0~M6) 완성.** Neon/Render/Vercel 배포, 실제 Map ID, 예산 알림, 실기기(PC+안드로이드/크롬) 테스트 통과. SPA 새로고침 404·모바일 한글 입력 깨짐 발견/수정. 삼성 인터넷 이슈는 미해결 보류. | [2026-09-06.md](2026-09-06.md) |
| 2026-09-07 | 사용자 관점 기능 백로그 정리(`docs/PROJECT.md`에 Tier 1/2/3 + 인프라 안전장치 신설). 코드 확인으로 삭제/날짜자동생성/예산 필드의 기존 구현 여부 확정. 코드 변경 없음. 이후 같은 날 로그 구조를 폴더(`logs/<날짜>/log.md`)에서 평면 파일(`logs/<날짜>.md`)로 변경. 이어서 **M7 착수** — "여행 생성 시 날짜 범위 → Day 자동 생성" 백엔드 구현(`POST /api/trips`가 단일 트랜잭션으로 Day까지 생성) 및 curl 검증 완료. 이어서 **M7 Tier 1 프론트 3건 완료** — `TripListPage.tsx`에 삭제 버튼(confirm)·인라인 수정 폼(이름/목적지/기간, Day 비동기화 안내)·생성 폼 날짜 범위 입력(둘 다 입력 강제, 422 에러 메시지 표시) 추가, `api/trips.ts`에 `updateTrip` 신설 및 `createTrip` 반환 타입을 `TripDetail`로 수정. 빌드 통과, curl로 API 계약 일치 확인(브라우저 수동 클릭 테스트는 미완료). 이어서 **qa-reviewer 지적 수정** — `deleteMutation`에 `onError` 핸들러 누락(삭제 실패 시 무피드백) 발견/수정, 기존 `extractErrorMessage()` 재사용해 해당 항목 근처에 에러 표시. 빌드 통과, 백엔드 임시 기동 후 401/404 실패 케이스를 API 레벨로 재현·확인. 이어서 **qa-reviewer 지적 수정(백엔드)** — `PATCH /api/trips/{id}`에 날짜 범위 검증이 없어 역전·366일 기간이 200으로 통과되던 문제 해결. 검증 규칙을 `validate_date_range()` 순수 함수로 추출해 POST(스키마)/PATCH(라우터)가 공유하고, PATCH는 DB 기존 값과 병합 후 검증 → 422. curl 12케이스(회귀 포함) 통과. | [2026-09-07.md](2026-09-07.md) |
| 2026-09-08 | **M7 Tier 1 프론트 2건 완료(ui-dev)** — (1) Day 탭 자동 라벨링: `label` 없는 Day에 `Day{순번} - MM/DD(요일)` 자동 표시(`DayEditorPage.tsx`), 사용자 지정 `label`은 그대로 우선. (2) 일정 제목 인라인 수정: `api/items.ts`에 `updateItem` 추가, `ItineraryItemCard.tsx`에 클릭/연필 아이콘 → 인라인 편집 → Enter/저장 UI 추가, 빈 문자열 저장 차단. 둘 다 백엔드 변경 없음(기존 API 그대로 사용). 빌드 통과, Node로 요일 계산 검증, 백엔드 임시 기동해 curl로 API 계약 확인(브라우저 수동 클릭은 미완료). 이어서 **qa-reviewer 지적 수정(junior-dev)** — `ItineraryItemCreate`/`ItineraryItemUpdate`에 `title` 빈 값/공백 검증 누락 발견/수정. `field_validator`로 trim 후 빈 문자열 거부(422), `ItineraryItemUpdate`는 `title` 미포함(None)은 통과시키되 명시적 빈 값만 거부. trim 정규화도 함께 적용. curl 7케이스(회귀 포함) 검증 완료. 이어서 **qa-reviewer 재검증 지적 수정(junior-dev)** — 위 수정의 `if value is None: return value` 조기 리턴이 "필드 생략"과 "명시적 `{"title": null}` 전송"을 구분 못해 후자도 통과시켜 DB `NOT NULL` 위반으로 500이 나던 버그 발견/수정. None도 빈 문자열과 동일하게 422로 거부하도록 통일. TestClient 5케이스(회귀 포함) 검증 완료. 이어서 **모바일 연속 스크롤 일정 뷰(트리플 스타일) 설계 확정(senior-dev, 코드 변경 없음)** — 데이터는 백엔드 신설 엔드포인트 대신 `useQueries` Day별 병렬 조회(`useTripItems` 훅으로 캡슐화)로 결정, 데스크톱/모바일은 Google Maps 이중 마운트(Map load 이중 과금) 회피를 위해 CSS가 아닌 JS 미디어쿼리로 택일 렌더, 활성 Day 추적은 고정 블록 높이를 `ResizeObserver`로 측정해 `rootMargin`을 잡는 IntersectionObserver 스크롤 스파이. Day 라벨은 `Day1 9/10(목)`으로 확정. ui-dev용 구현 스펙 작성 완료(구현은 다음 세션). 이어서 **모바일 연속 스크롤 일정 뷰 구현 완료(ui-dev)** — 스펙 그대로 신규 파일 7개(`lib/dayLabel.ts` 등) 추가하고 `DayEditorPage.tsx`를 컨테이너로 리팩터링, 데스크톱은 동작 변화 없이 유지·모바일은 지도 상단 고정+연속 스크롤+IntersectionObserver 스크롤 스파이로 전환. `MapView`/`ItineraryList`/`ItineraryItemCard`/`AddItemModal`/API/백엔드는 무변경. 빌드 통과, curl+Node로 라벨 포맷/모듈 트랜스폼 검증(브라우저 자동화 도구 없어 실제 스크롤·핀 전환은 미확인, 사용자 확인 필요). 이어서 **qa-reviewer 지적 수정(ui-dev)** — `DayEditorDesktop.tsx`가 로딩 중에도 `<ItineraryList>`에 빈 배열을 넘겨 "불러오는 중..."과 "등록된 일정이 없습니다"가 동시에 잠깐 보이는 false empty state 회귀 발견/수정. `!itemsLoading &&`로 게이팅해 원본(`{items && (...)}`undefined 방어)과 동일한 동작으로 복구, 지도는 원본처럼 로딩 중에도 그대로 렌더. `DayEditorMobile.tsx`는 이미 전체 Day 섹션이 `itemsLoading`으로 게이팅돼있어 동일 회귀 없음(코드 변경 없음). 빌드 통과, 코드 추적으로 회귀 없음 확인. qa 발견 사소 이슈 3건(`isError` 미사용, ADR `isPending`/코드 `isLoading` 불일치, `registerSection` 언마운트 미처리)은 `docs/PROJECT.md` Tier 2에 백로그만 기록(미수정). | [2026-09-08.md](2026-09-08.md) |
| 2026-09-09 | **M7 Tier 1 마지막 항목 "여행 기간(PATCH) 수정 시 Day 동기화" 백엔드 완료(senior-dev)** — 설계 결정을 이 프로젝트 첫 ADR([`docs/adr/0001-patch-trip-date-day-sync.md`](../docs/adr/0001-patch-trip-date-day-sync.md), `docs/adr/` 신설)로 기록. **PATCH는 "추가만 하고 절대 삭제하지 않는" 비파괴 연산**으로 고정하고(Day→ItineraryItem이 CASCADE라 자동 삭제는 곧 일정 손실), 범위 밖 Day는 응답 `out_of_range_days`(+`item_count`)로 보고만 해서 프론트가 확인받은 뒤 **기존 `DELETE /api/days/{id}`**로 지우는 2단계 방식 채택(새 엔드포인트 0개). 후보 `force=true`(파괴적 동작을 일반 수정에 묶어 클라 실수 한 줄이 곧 손실, 경고하려면 어차피 사전 조회 필요)와 "남겨두고 표시만"(정리 수단 없음)은 기각. 구현: `diff_days_for_range()`/`DayRangeDiff`/`MAX_TRIP_TOTAL_DAYS`(=180, 기간 이동 PATCH 반복 시 Day 무한 누적 방지) 추가(`services/trip_days.py`), `TripUpdateResult`/`OutOfRangeDayRead` 추가(`schemas.py`, `TripRead` 상위집합이라 기존 프론트 호환), `update_trip`에 동기화 로직(기존 `validate_date_range()` 흐름은 그대로 두고 뒤에 이어붙임, 요청에 날짜 키가 있을 때만 동기화). DB 스키마 변경 없어 마이그레이션 불필요. TestClient 8시나리오 46체크 통과(임시 SQLite로 실행, `dev.db` 무변경). **같은 날 프론트 완료(ui-dev)** — `types/models.ts`에 `OutOfRangeDay`/`TripUpdateResult` 추가, `api/trips.ts`의 `updateTrip()` 반환 타입 확장. 신규 `components/OutOfRangeDaysModal.tsx`(범위 밖 Day + `item_count` 경고 표시, "삭제"는 대상 Day마다 기존 `deleteDay()`를 `Promise.allSettled`로 병렬 호출해 부분 실패 허용·실패 항목만 에러 표시, "유지"는 삭제 없이 닫기)와 `lib/errors.ts`(신규, 기존 `extractErrorMessage()` 공유 위치로 이동) 추가. `TripListPage.tsx`의 `TripEditForm`이 저장 성공 시 범위 밖 Day가 있으면 이 모달을, 추가된 Day만 있으면 "N일이 추가되었습니다" 인라인 안내를 띄우도록 변경, 안내 문구도 새 동작에 맞게 갱신. `DayEditorMobile.tsx`/`lib/dayLabel.ts`는 무변경(스코프 밖). `npm run build` 통과, 브라우저 수동 클릭 테스트는 미수행. **M7 Tier 1 백로그 전 항목 백엔드+프론트 완료.** 커밋 안 함(사용자 확인 필요 항목은 상세 로그 참고). **이어서 qa-reviewer 검증** — API 레벨 재현 9시나리오 전부 기대대로 통과, 치명적 이슈 없음. 보통 1건(`Day(trip_id, date)`에 DB 유니크 제약이 없어 동시 PATCH 시 중복 Day 생성 가능 — 2인 공유 앱 전제와 맞물리는 레이스 컨디션, 미수정 리포트만), 사소 1건(`OutOfRangeDaysModal`이 삭제 전부 실패해도 캐시 무효화 — 낭비성 refetch, 기능 문제 없음). 캐시 무효화 범위(`DayEditorPage.tsx`)는 회귀 없음 확인. `npm run build` 재통과. **이어서 [보통] 이슈 수정(senior-dev)** — `Day`에 `(trip_id, date)` 유니크 제약(`uq_day_trip_id_date`) + Alembic `73a697fa5fee` 추가(중복 사전 확인 가드 포함, 중복 있으면 일부러 실패). 위반은 `update_trip`(트랜잭션 전체 롤백)·`create_day`·`update_day`에서 409로 변환하고 **자동 재시도는 안 함**(상대가 기간까지 바꿨을 수 있어 재시도가 곧 last-write-wins). `MAX_TRIP_TOTAL_DAYS` 동시 우회는 락 대신 알려진 한계로 문서화(SQLite에서 `FOR UPDATE`가 무시돼 검증 불가, 유니크 제약 덕에 최대 270에서 멈추고 자가 수복). 근거는 후속 [ADR-0002](../docs/adr/0002-day-unique-constraint-and-conflict-handling.md). TestClient 58체크 통과(기존 9시나리오 회귀 포함), `dev.db` 마이그레이션 적용/롤백 왕복 + 데이터 무손실 확인. 운영 Neon에 `alembic upgrade head` 필요. **이어서 qa-reviewer 재검증에서 나온 [보통] 이슈 수정(junior-dev)** — `DayEditorPage.tsx`의 "날짜 추가" 폼이 409 등 에러를 완전히 무시하던 문제를 `onError`+`extractErrorMessage()`로 해결, ADR-0002/로그의 "이 API를 부르는 화면이 없다"는 부정확한 서술도 정정. 빌드 통과. **이어서 배포** — 운영 Neon DB에 `alembic upgrade head` 시도 시 트립 6번("테스트")에서 실제 중복 Day 발견(설계대로 안전하게 실패), 일정 데이터 병합(Day 10→Day 6 이동) 후 재실행 성공. 커밋(`93fde66`)+`git push origin main` 완료, Render/Vercel 자동 재배포 시작. **이어서 M7 Tier 1 "일정 메모/체크리스트" 백엔드 완료(senior-dev)** — 사용자 확인으로 범위를 "여행(Trip) 단위 체크리스트(항목별 체크박스+삭제)"로 확정. 새 모델 `ChecklistItem`(`trip_id` FK CASCADE/`text`/`is_checked`/`created_at`) + 마이그레이션 `d6eea980ecbe`, `ChecklistItemCreate/Update/Read`(`schemas.py`), 신규 라우터 `app/routers/checklist.py`(`GET|POST /api/trips/{id}/checklist`, `PATCH|DELETE /api/checklist/{itemId}`, 세션 쿠키 보호). 설계 근거는 [ADR-0003](../docs/adr/0003-trip-checklist-model.md) — **정렬 컬럼을 일부러 두지 않고 생성 순서(`id`) 고정**(준비물은 순서에 의미가 없고, 나중에 필요하면 `position DEFAULT 0` 컬럼 하나로 백필 없이 확장 가능 → 지금 안 넣는 쪽이 되돌리기 싸다), **토글은 전용 엔드포인트가 아닌 멱등한 일반 PATCH**(`toggle`은 재시도/더블탭이 상태 반전 2회가 됨), 트립당 100개+텍스트 200자 상한(개수만 있으면 무의미해서 세트로). `ItineraryItemUpdate.title`에서 겪은 "필드 생략 vs 명시적 null" 버그를 `text`·`is_checked` 양쪽에서 방어. TestClient 61체크 통과(인증 401·트립 삭제 CASCADE·개수 상한 포함), 마이그레이션 왕복 확인, `dev.db` 데이터 무변경. **이어서 같은 날 프론트 완료(ui-dev)** — `types/models.ts`에 `ChecklistItem`, 신규 `api/checklist.ts`(4개 함수, `api/days.ts` 스타일), 신규 `components/TripChecklist.tsx`를 `TripListPage.tsx`의 각 여행 카드 안에 배치(모달 아닌 인라인 펼치기 섹션). 접힌 상태에서는 `enabled: isOpen`으로 API를 호출하지 않되, React Query가 `enabled`와 무관하게 같은 쿼리 키의 캐시를 반환하는 특성을 이용해 한 번 펼친 뒤로는 캐시로 "준비물 N/M" 배지를 유지(추가 fetch 없음, 별도 상태 복제도 불필요). 체크 토글은 낙관적 업데이트+롤백, 추가/삭제는 응답으로 캐시 갱신, 빈 값/200자 초과는 클라이언트에서 선차단. `npm run build` 통과. **M7 Tier 1 "일정 메모/체크리스트" 백엔드+프론트 모두 완료.** 운영 Neon `alembic upgrade head`(`d6eea980ecbe`) 필요, 브라우저 실기 확인 미수행, 커밋 안 함(사용자 확인 필요, `logs/2026-09-09.md` 참고). **이어서 qa-reviewer 1차 검증** — 독립 TestClient(인메모리 SQLite) 27체크 전부 통과(텍스트 검증/`is_checked` null 방어/100개 상한 경계/트립 간 격리/401/CASCADE/부분 업데이트), 프론트는 코드 리뷰로 낙관적 업데이트 롤백·`enabled: isOpen`·캐시 키 격리·클라이언트 200자 경계·`TripListPage.tsx` 무회귀(`git diff` 2줄) 확인, `npm run build` 재통과. 치명적/보통 이슈 없음, 사소 1건(카드를 연 직후 초기 GET 완료 전에 항목을 추가하면 뒤늦은 GET 응답이 화면에서 일시적으로 덮어씀 — 서버 데이터 손실 없음, 미수정 리포트만). | [2026-09-09.md](2026-09-09.md) |
